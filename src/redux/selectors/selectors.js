export const tokenSelect = (state) => state.token;
export const userSelect = (state) => state.user;
export const loaderSelect = (state) => state.loader;
export const errorSelect = (state) => state.error;
export const currentStateSelect = (state) => state.currentState;
